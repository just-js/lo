const api = {
  test: {
    parameters: [],
    result: 'i32',
    name: 'test'
  }
}

const constants = {}
const includes = []
const name = 'core'

export { api, includes, name, constants }
