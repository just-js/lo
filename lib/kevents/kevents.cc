
// [do not edit,<auto-generated />]
// This file has been automatically generated, please do not change unless you disable auto-generation in the Makefile  
#include <sys/event.h>

#include <sys/types.h>

#include <sys/time.h>






#include <lo.h>

namespace lo {
namespace kevents {

using v8::FunctionCallbackInfo;
using v8::Local;
using v8::ObjectTemplate;
using v8::Isolate;
using v8::Value;
using v8::Integer;
using v8::Number;
using v8::FunctionTemplate;
using v8::FunctionCallback;
using v8::CFunction;
using v8::CTypeInfo;
using v8::Uint8Array;
using v8::CFunctionInfo;
using v8::String;
using v8::Uint32Array;
using v8::ArrayBuffer;
using v8::Context;
using v8::Function;
using v8::Object;
using v8::HandleScope;
using v8::BigInt;


#ifdef __linux__

#endif
#ifdef __MACH__

#endif

int32_t kqueueFast(void* p);
CTypeInfo cargskqueue[1] = {
  CTypeInfo(CTypeInfo::Type::kV8Value),

};
CTypeInfo rckqueue = CTypeInfo(CTypeInfo::Type::kInt32);
CFunctionInfo infokqueue = CFunctionInfo(rckqueue, 1, cargskqueue);
CFunction pFkqueue = CFunction((const void*)&kqueueFast, &infokqueue);

int32_t keventFast(void* p, int32_t p0, uint64_t* p1, int32_t p2, uint64_t* p3, int32_t p4, uint64_t* p5);
CTypeInfo cargskevent[7] = {
  CTypeInfo(CTypeInfo::Type::kV8Value),
  CTypeInfo(CTypeInfo::Type::kInt32),
  CTypeInfo(CTypeInfo::Type::kUint64),
  CTypeInfo(CTypeInfo::Type::kInt32),
  CTypeInfo(CTypeInfo::Type::kUint64),
  CTypeInfo(CTypeInfo::Type::kInt32),
  CTypeInfo(CTypeInfo::Type::kUint64),
};
CTypeInfo rckevent = CTypeInfo(CTypeInfo::Type::kInt32);
CFunctionInfo infokevent = CFunctionInfo(rckevent, 7, cargskevent);
CFunction pFkevent = CFunction((const void*)&keventFast, &infokevent);

int32_t kevent64Fast(void* p, int32_t p0, uint64_t* p1, int32_t p2, uint64_t* p3, int32_t p4, uint32_t p5, uint64_t* p6);
CTypeInfo cargskevent64[8] = {
  CTypeInfo(CTypeInfo::Type::kV8Value),
  CTypeInfo(CTypeInfo::Type::kInt32),
  CTypeInfo(CTypeInfo::Type::kUint64),
  CTypeInfo(CTypeInfo::Type::kInt32),
  CTypeInfo(CTypeInfo::Type::kUint64),
  CTypeInfo(CTypeInfo::Type::kInt32),
  CTypeInfo(CTypeInfo::Type::kUint32),
  CTypeInfo(CTypeInfo::Type::kUint64),
};
CTypeInfo rckevent64 = CTypeInfo(CTypeInfo::Type::kInt32);
CFunctionInfo infokevent64 = CFunctionInfo(rckevent64, 8, cargskevent64);
CFunction pFkevent64 = CFunction((const void*)&kevent64Fast, &infokevent64);

#ifdef __linux__

#endif
#ifdef __MACH__

#endif

void kqueueSlow(const FunctionCallbackInfo<Value> &args) {

  int32_t rc = kqueue();
  args.GetReturnValue().Set(rc);
}

int32_t kqueueFast(void* p) {

  return kqueue();
}
void keventSlow(const FunctionCallbackInfo<Value> &args) {
  int32_t v0 = Local<Integer>::Cast(args[0])->Value();
  const struct kevent * v1 = reinterpret_cast<const struct kevent *>((uint64_t)Local<Integer>::Cast(args[1])->Value());
  int32_t v2 = Local<Integer>::Cast(args[2])->Value();
  struct kevent * v3 = reinterpret_cast<struct kevent *>((uint64_t)Local<Integer>::Cast(args[3])->Value());
  int32_t v4 = Local<Integer>::Cast(args[4])->Value();
  const struct timespec * v5 = reinterpret_cast<const struct timespec *>((uint64_t)Local<Integer>::Cast(args[5])->Value());
  int32_t rc = kevent(v0, v1, v2, v3, v4, v5);
  args.GetReturnValue().Set(rc);
}

int32_t keventFast(void* p, int32_t p0, uint64_t* p1, int32_t p2, uint64_t* p3, int32_t p4, uint64_t* p5) {
  int32_t v0 = p0;
  const struct kevent * v1 = reinterpret_cast<const struct kevent *>(p1);
  int32_t v2 = p2;
  struct kevent * v3 = reinterpret_cast<struct kevent *>(p3);
  int32_t v4 = p4;
  const struct timespec * v5 = reinterpret_cast<const struct timespec *>(p5);
  return kevent(v0, v1, v2, v3, v4, v5);
}
void kevent64Slow(const FunctionCallbackInfo<Value> &args) {
  int32_t v0 = Local<Integer>::Cast(args[0])->Value();
  const struct kevent64_s * v1 = reinterpret_cast<const struct kevent64_s *>((uint64_t)Local<Integer>::Cast(args[1])->Value());
  int32_t v2 = Local<Integer>::Cast(args[2])->Value();
  struct kevent64_s * v3 = reinterpret_cast<struct kevent64_s *>((uint64_t)Local<Integer>::Cast(args[3])->Value());
  int32_t v4 = Local<Integer>::Cast(args[4])->Value();
  uint32_t v5 = Local<Integer>::Cast(args[5])->Value();
  const struct timespec * v6 = reinterpret_cast<const struct timespec *>((uint64_t)Local<Integer>::Cast(args[6])->Value());
  int32_t rc = kevent64(v0, v1, v2, v3, v4, v5, v6);
  args.GetReturnValue().Set(rc);
}

int32_t kevent64Fast(void* p, int32_t p0, uint64_t* p1, int32_t p2, uint64_t* p3, int32_t p4, uint32_t p5, uint64_t* p6) {
  int32_t v0 = p0;
  const struct kevent64_s * v1 = reinterpret_cast<const struct kevent64_s *>(p1);
  int32_t v2 = p2;
  struct kevent64_s * v3 = reinterpret_cast<struct kevent64_s *>(p3);
  int32_t v4 = p4;
  uint32_t v5 = p5;
  const struct timespec * v6 = reinterpret_cast<const struct timespec *>(p6);
  return kevent64(v0, v1, v2, v3, v4, v5, v6);
}
#ifdef __linux__

#endif
#ifdef __MACH__

#endif
void Init(Isolate* isolate, Local<ObjectTemplate> target) {
  Local<ObjectTemplate> module = ObjectTemplate::New(isolate);
  SET_FAST_METHOD(isolate, module, "kqueue", &pFkqueue, kqueueSlow);
  SET_FAST_METHOD(isolate, module, "kevent", &pFkevent, keventSlow);
  SET_FAST_METHOD(isolate, module, "kevent64", &pFkevent64, kevent64Slow);

#ifdef __linux__

#endif
#ifdef __MACH__

#endif
  SET_VALUE(isolate, module, "EVFILT_READ", Integer::New(isolate, (int32_t)EVFILT_READ));
  SET_VALUE(isolate, module, "EVFILT_EXCEPT", Integer::New(isolate, (int32_t)EVFILT_EXCEPT));
  SET_VALUE(isolate, module, "EVFILT_WRITE", Integer::New(isolate, (int32_t)EVFILT_WRITE));
  SET_VALUE(isolate, module, "EVFILT_VNODE", Integer::New(isolate, (int32_t)EVFILT_VNODE));
  SET_VALUE(isolate, module, "EVFILT_PROC", Integer::New(isolate, (int32_t)EVFILT_PROC));
  SET_VALUE(isolate, module, "EVFILT_SIGNAL", Integer::New(isolate, (int32_t)EVFILT_SIGNAL));
  SET_VALUE(isolate, module, "EVFILT_MACHPORT", Integer::New(isolate, (int32_t)EVFILT_MACHPORT));
  SET_VALUE(isolate, module, "EVFILT_TIMER", Integer::New(isolate, (int32_t)EVFILT_TIMER));
  SET_VALUE(isolate, module, "EV_ADD", Integer::New(isolate, (int32_t)EV_ADD));
  SET_VALUE(isolate, module, "EV_ENABLE", Integer::New(isolate, (int32_t)EV_ENABLE));
  SET_VALUE(isolate, module, "EV_DISABLE", Integer::New(isolate, (int32_t)EV_DISABLE));
  SET_VALUE(isolate, module, "EV_DELETE", Integer::New(isolate, (int32_t)EV_DELETE));
  SET_VALUE(isolate, module, "EV_RECEIPT", Integer::New(isolate, (int32_t)EV_RECEIPT));
  SET_VALUE(isolate, module, "EV_ONESHOT", Integer::New(isolate, (int32_t)EV_ONESHOT));
  SET_VALUE(isolate, module, "EV_CLEAR", Integer::New(isolate, (int32_t)EV_CLEAR));
  SET_VALUE(isolate, module, "EV_EOF", Integer::New(isolate, (int32_t)EV_EOF));
  SET_VALUE(isolate, module, "EV_OOBAND", Integer::New(isolate, (int32_t)EV_OOBAND));
  SET_VALUE(isolate, module, "EV_ERROR", Integer::New(isolate, (int32_t)EV_ERROR));
  SET_VALUE(isolate, module, "KEVENT_FLAG_IMMEDIATE", Integer::New(isolate, (int32_t)KEVENT_FLAG_IMMEDIATE));

#ifdef __linux__

#endif
#ifdef __MACH__

#endif

#ifdef __MACH__
  SET_VALUE(isolate, module, "struct_kevent64_s_size", Integer::New(isolate, sizeof(kevent64_s)));
  SET_VALUE(isolate, module, "struct_time_t_size", Integer::New(isolate, sizeof(time_t)));
  SET_VALUE(isolate, module, "struct_timespec_size", Integer::New(isolate, sizeof(timespec)));

#endif
#ifdef __linux__
  SET_VALUE(isolate, module, "struct_kevent64_s_size", Integer::New(isolate, sizeof(kevent64_s)));
  SET_VALUE(isolate, module, "struct_time_t_size", Integer::New(isolate, sizeof(time_t)));
  SET_VALUE(isolate, module, "struct_timespec_size", Integer::New(isolate, sizeof(timespec)));

#endif
  SET_MODULE(isolate, target, "kevents", module);
}
} // namespace kevents
} // namespace lo

extern "C"  {
  DLL_PUBLIC void* _register_kevents() {
    return (void*)lo::kevents::Init;
  }
}
